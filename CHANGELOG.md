# Changelog

このファイルは、Date Bookmark Manager の主な変更内容をバージョンごとに記録します。
形式は [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/) を参考にしています。

## [1.3.0] - 2026-06-26

### Added
- **ツールバーのポップアップを追加**しました。拡張機能アイコンをクリックして「このページを今日のフォルダに保存」ボタンから、右クリックを使わずにワンクリックで保存できます。
- **初回インストール時に使い方ページ（welcome.html）を自動表示**するようにしました。インストール直後に操作方法が分からず離脱してしまう問題への対策です。
- 開発支援のためのドネイトリンク（Buy me a coffee）をオプション画面・ポップアップ・ウェルカムページに追加しました。
- 拡張機能の改善のため、Google Analytics (GA4) による匿名の利用統計の収集を追加しました。
  - 収集するのは機能の利用イベント（ブックマーク保存・設定画面表示など）と、おおまかな国・デバイス種別のみです。個人を特定できる情報やブックマークの内容・URLは一切送信しません。
  - オプション画面の「匿名の利用統計を送信して開発を支援する」のチェックを外すことで、いつでも送信を停止（オプトアウト）できます。
- API secret はリポジトリに含めない `config.js`（`.gitignore` 対象）へ分離しました。

## [1.2.2] - 2026-06-02

### Added
- オプション設定「当月分も月別フォルダにまとめる (先に常にまとめる)」を追加しました。
  - 有効にすると、ブックマーク保存時に当月の月フォルダ配下に日付フォルダが作成され、整理時にも当月フォルダが整理対象となります。

## [1.2.1] - 2026-04-29

### Fixed
- 拡張機能のインストール時 / 更新時 / Chrome 本体の更新時に、ユーザーがブックマークを 1 件も追加していないにもかかわらず空の日付フォルダが作成されてしまう不具合を修正しました。
  - `chrome.runtime.onInstalled` 内で当日フォルダを先行作成していたロジック (`createTodayFolderIfNotExists`) を削除し、当日フォルダはユーザーが右クリックメニューからブックマークを実際に保存したタイミングでのみ作成されるようにしました。

## [1.2.0]

### Added
- オプション画面を追加しました。
  - 日付フォーマットを 6 種類（`yyyy-MM-dd` / `yyyyMMdd` / `yyyy/MM/dd` / `MMDD` / `MM-DD` / `MM/DD`）から選べるようになりました。
  - 右クリックメニューの表示言語を 日本語 / English / 简体中文 / 한국어 から選べるようになりました。

## [1.0.0]

### Added
- 初回リリース。
- 右クリックメニュー「日付フォルダにブックマークする」から、当日の日付フォルダ（`YYYY-MM-DD`）にブックマークを保存する機能。
- 月が変わった際に前月分の日付フォルダを「`YYYY-MM`」形式の月別フォルダへ自動的に集約する機能。
